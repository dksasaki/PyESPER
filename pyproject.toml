[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "PyESPER"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "numpy>=1.26.0,<1.27",
    "xarray",
    "netcdf4",
]

[tool.setuptools.packages.find]
include = ["PyESPER*"]