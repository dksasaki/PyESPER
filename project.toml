[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "pyesper"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "numpy==1.26.0.*",
    "xarray",
    "netcdf4",
]